/*
 * mmu.h — ARMv8-A 4 seviyeli sayfa tabloları (4KB granül)
 * (Planda "MEVCUT" sayılan temel bileşen)
 */
#ifndef TOROS_MMU_H
#define TOROS_MMU_H

#include <toros/types.h>

void mmu_init(void);

#endif
